#include <stdio.h>
#include <stdlib.h>
#define LEN 4

float ** d(float **matrix, int n){
  float **d = 
}

int main(){
  float *b = malloc(LEN*sizeof(float));
  float *solus = malloc(LEN*sizeof(float));
  float ** matrix = NULL;
  float **matrix = (float**) malloc(LEN*sizeof(float*));
  for (i = 0; i < LEN; i++){
    matrix[i] = (float*) malloc(LEN*sizeof(float));
  }

  return 0;
}
